name: DevOps / PR Hygiene

on:
  pull_request:
    types: [opened, edited, synchronize, reopened, ready_for_review]
  pull_request_target:
    types: [opened, synchronize, reopened]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  semantic-pr:
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
    uses: ffreis-org/ffreis-workflows-general/.github/workflows/general-semantic-pr.yml@caa797cc7baec52e99f21b86f532a4b7c88cf4fa # fix/grype-install-outside-scan-path

  actionlint:
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
    uses: ffreis-org/ffreis-workflows-general/.github/workflows/general-actionlint.yml@caa797cc7baec52e99f21b86f532a4b7c88cf4fa # fix/grype-install-outside-scan-path
    with:
      working-directory: "."

  labeler:
    if: github.event_name == 'pull_request_target'
    permissions:
      contents: read
      pull-requests: write
      issues: write
    uses: ffreis-org/ffreis-workflows-general/.github/workflows/general-labeler.yml@caa797cc7baec52e99f21b86f532a4b7c88cf4fa # fix/grype-install-outside-scan-path
    with:
      rules-json: |-
        [
          {"label":"ci","patterns":[".github/workflows/**",".github/actions/**"]},
          {"label":"vagrant","patterns":["Vagrantfile","**/*.rb"]},
          {"label":"scripts","patterns":["scripts/**","**/*.sh","tests/**"]},
          {"label":"docs","patterns":["**/*.md"]}
        ]
